name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Instalar dependências
        run: npm install

      - name: Rodar testes
        run: npm test
        env:
          SUPABASE_URL: ${{ https://macqnjohigrqnuzwlzak.supabase.co }}
          SUPABASE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1hY3Fuam9oaWdycW51endsemFrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc1NjkyNDAsImV4cCI6MjEwMzE0NTI0MH0.r6IYdxAzhVnKowsycih7QIZ_iWbidABJ42-fKHcw3-c
 }}
          JWT_SECRET: ${{ osanimaissaoasmelhorescriaturasdomundo
 }}